export type configVar = {
    validUsername: string;
    validPassword: string;

    inventoryItemName: string;

    firstName: string;
    lastName: string;
    postalCode: string;

    taxRate: number;
}